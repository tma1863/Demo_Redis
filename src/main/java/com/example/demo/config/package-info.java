/**
 * Framework-level configuration (Redis connection, cache manager, etc.).
 * Populated in a later phase; kept separate from the feature packages so the
 * caching/infrastructure wiring stays out of the domain code.
 */
package com.example.demo.config;
